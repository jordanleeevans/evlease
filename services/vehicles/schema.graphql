type Vehicle @key(fields: "id") {
    id: ID!
    make: String!
    model: String!
    year: Int!
    trim: String
    colour: String
    imageUrl: String
    rangeMiles: Int
    batterySizeKwh: Float
    available: Boolean!
}

type Query {
    vehicles: [Vehicle!]!
    vehicle(id: ID!): Vehicle
}